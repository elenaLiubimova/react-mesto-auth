import React from "react";
import RegAuthForm from "./RegAuthForm";

const Register = ({ title, buttonText }) => {
  return <RegAuthForm title="Регистрация" buttonText="Зарегистрироваться"  />;
};

export default Register;
